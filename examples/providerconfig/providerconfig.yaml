apiVersion: kind.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
# The KIND provider does not require credentials.
# It uses the local Docker daemon which is accessible without authentication.
spec: {}
