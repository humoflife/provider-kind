---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusters.kind.crossplane.io
spec:
  group: kind.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - kind
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the KIND clusters API. A Cluster represents
          a KIND (Kubernetes IN Docker) cluster managed by the provider-kind Crossplane
          provider.
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation
              of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this
              object represents.
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of a Cluster.
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ClusterParameters defines the desired state of a KIND
                  cluster.
                properties:
                  containerdConfigPatches:
                    description: ContainerdConfigPatches are toml-encoded patches
                      to apply to all node containerd configs.
                    items:
                      type: string
                    type: array
                  featureGates:
                    additionalProperties:
                      type: boolean
                    description: FeatureGates is a map of Kubernetes feature gate
                      names to boolean values, passed to the cluster via kubeadm.
                    type: object
                  kubeProxyMode:
                    description: KubeProxyMode sets the kube-proxy mode for the cluster.
                    enum:
                    - iptables
                    - ipvs
                    - nftables
                    - none
                    type: string
                  networking:
                    description: Networking defines cluster-wide networking configuration.
                    properties:
                      apiServerAddress:
                        description: APIServerAddress is the IP address on the host
                          to listen on for the Kubernetes API server.
                        type: string
                      apiServerPort:
                        description: APIServerPort is the port on the host to listen
                          on for the Kubernetes API server.
                        format: int32
                        type: integer
                      disableDefaultCNI:
                        description: DisableDefaultCNI disables the default kindnetd
                          CNI plugin so that an alternative CNI can be installed.
                        type: boolean
                      ipFamily:
                        description: IPFamily is the IP address family for the cluster.
                        enum:
                        - ipv4
                        - ipv6
                        - dual
                        type: string
                      kubeProxyMode:
                        description: KubeProxyMode sets the kube-proxy mode for the
                          cluster.
                        enum:
                        - iptables
                        - ipvs
                        - nftables
                        - none
                        type: string
                      podSubnet:
                        description: PodSubnet is the CIDR block for pod networking.
                        type: string
                      serviceSubnet:
                        description: ServiceSubnet is the CIDR block for service
                          networking.
                        type: string
                    type: object
                  nodes:
                    description: Nodes defines the nodes in the cluster.
                    items:
                      description: Node defines a KIND cluster node.
                      properties:
                        extraMounts:
                          description: ExtraMounts are additional directory or file
                            mounts from the host into the node container.
                          items:
                            description: Mount defines a bind mount from the host
                              into a KIND node container.
                            properties:
                              containerPath:
                                description: ContainerPath is the path inside the
                                  node container to mount to.
                                type: string
                              hostPath:
                                description: HostPath is the absolute path on the
                                  host to mount.
                                type: string
                              propagation:
                                description: Propagation sets the mount propagation
                                  mode.
                                enum:
                                - None
                                - HostToContainer
                                - Bidirectional
                                type: string
                              readonly:
                                description: Readonly makes the mount read-only inside
                                  the container.
                                type: boolean
                              selinuxRelabel:
                                description: SelinuxRelabel enables SELinux relabeling
                                  on the mounted directory.
                                type: boolean
                            required:
                            - containerPath
                            - hostPath
                            type: object
                          type: array
                        extraPortMappings:
                          description: ExtraPortMappings are additional port mappings
                            from the node container to the host machine.
                          items:
                            description: PortMapping defines a port forwarding from
                              the node container to the host.
                            properties:
                              containerPort:
                                description: ContainerPort is the port inside the
                                  node container.
                                format: int32
                                type: integer
                              hostPort:
                                description: HostPort is the port on the host machine.
                                format: int32
                                type: integer
                              listenAddress:
                                description: ListenAddress is the host IP address
                                  to bind the port on.
                                type: string
                              protocol:
                                description: Protocol is the network protocol for
                                  the port mapping.
                                enum:
                                - TCP
                                - UDP
                                - SCTP
                                type: string
                            required:
                            - containerPort
                            - hostPort
                            type: object
                          type: array
                        image:
                          description: Image is the node container image to use.
                          type: string
                        kubeadmConfigPatches:
                          description: KubeadmConfigPatches are kubeadm config patches
                            applied to nodes during cluster creation.
                          items:
                            type: string
                          type: array
                        labels:
                          additionalProperties:
                            type: string
                          description: Labels are additional labels to apply to the
                            node.
                          type: object
                        role:
                          default: control-plane
                          description: Role is the node role in the cluster.
                          enum:
                          - control-plane
                          - worker
                          type: string
                      required:
                      - role
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  runtimeConfig:
                    additionalProperties:
                      type: string
                    description: RuntimeConfig is passed to the API server as --runtime-config
                      flags.
                    type: object
                  waitForReady:
                    description: WaitForReady is the duration to wait for the cluster
                      to become ready after creation (e.g. "5m", "30s").
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: ManagementPolicies specify the array of actions Crossplane
                  is allowed to take on the managed and external resources.
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ClusterStatus defines the observed state of a Cluster.
            properties:
              atProvider:
                description: ClusterObservation is the observable state of a KIND
                  cluster.
                properties:
                  apiServerEndpoint:
                    description: APIServerEndpoint is the address of the Kubernetes
                      API server.
                    type: string
                  nodes:
                    description: Nodes are the observed states of the cluster nodes.
                    items:
                      description: NodeObservation is the observed state of a KIND
                        cluster node.
                      properties:
                        image:
                          description: Image is the container image used for this
                            node.
                          type: string
                        ipAddress:
                          description: IPAddress is the IPv4 address of the node container.
                          type: string
                        ipv6Address:
                          description: IPv6Address is the IPv6 address of the node container.
                            Only populated for IPv6 or dual-stack clusters.
                          type: string
                        name:
                          description: Name is the Docker container name for this
                            node.
                          type: string
                        role:
                          description: Role is the node role (control-plane or worker).
                          type: string
                        status:
                          description: Status is the Docker container status.
                          type: string
                      required:
                      - name
                      - role
                      - status
                      type: object
                    type: array
                  ready:
                    description: Ready indicates whether the cluster is ready and
                      all nodes are running.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
